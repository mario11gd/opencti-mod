import type { Plugin } from '@envelop/core';
import type { GraphQLArmorCallbackConfiguration } from '@escape.tech/graphql-armor-types';
import { OperationDefinitionNode, ValidationContext } from 'graphql';
export type MaxDirectivesOptions = {
    n?: number;
    exposeLimits?: boolean;
    errorMessage?: string;
} & GraphQLArmorCallbackConfiguration;
export declare const maxDirectivesDefaultOptions: Required<MaxDirectivesOptions>;
declare class MaxDirectivesVisitor {
    readonly OperationDefinition: Record<string, any>;
    private readonly context;
    private readonly config;
    private readonly visitedFragments;
    constructor(context: ValidationContext, options?: MaxDirectivesOptions);
    onOperationDefinitionEnter(operation: OperationDefinitionNode): void;
    private countDirectives;
}
export declare const maxDirectivesRule: (options?: MaxDirectivesOptions) => (context: ValidationContext) => MaxDirectivesVisitor;
export declare const maxDirectivesPlugin: (options?: MaxDirectivesOptions) => Plugin;
export {};
